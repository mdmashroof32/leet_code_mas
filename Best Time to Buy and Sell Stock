class Solution {
    public int maxProfit(int[] prices) {
        int min = Integer.MAX_VALUE;
        int maxP = 0;

        for (int n : prices) {
            min = Math.min(min, n);
            maxP = Math.max(maxP, n - min);
        }

        return maxP;
    }
}
